import React from 'react';
import Layout from '../shared/Layout';

const Home = () => {
    return (
       <Layout>
           <h4>Welcome to the Plans App</h4>
       </Layout>
    );
};

export default Home;

//then goto layout.js inside shared